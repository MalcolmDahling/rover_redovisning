import { recipe } from '@vanilla-extract/recipes';

export const SliderStyle = recipe({
  base: {
    position: 'relative',
    minHeight: '100vh',
    height: '100%',
    width: '100%',
    minWidth: '100vw',
  },
});
